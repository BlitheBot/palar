{
  "name": "palar-vuln-server-fixture",
  "private": true,
  "version": "0.1.0",
  "description": "Intentionally vulnerable MCP server fixture for palar's own test suite. Not published.",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
